"user strict";
const category = 'toy';
//console.log ('https://someurl.com/' + category + '/' ); старый формат через +
console.log(`https://someurl.com/${category}/5`); //интерполяцыя через ``

const user = 'Ivan';
alert (`Привет, ${user}`);